export const metadata = {
  title: 'Medical Representative Area | MyClinicSoft',
  description: 'Medical representative registration and management area.',
};

export default function MedicalRepresentativesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
