# MongoDB Connection String
# Copy this file to .env.local and update with your actual MongoDB connection string
#
# For MongoDB Atlas (Cloud - Recommended):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/clinic-management?retryWrites=true&w=majority
#
# For Local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/clinic-management
#
# After creating .env.local, restart your Next.js development server

MONGODB_URI=

# Session Secret (REQUIRED in production)
# Generate a strong random string: openssl rand -base64 32
SESSION_SECRET=

# Multi-Tenant Configuration
# ROOT_DOMAIN: Your apex/root domain (e.g., myclinic.com)
# Used to extract subdomains for tenant identification
ROOT_DOMAIN=

# COOKIE_DOMAIN: Set to apex domain with leading dot for cross-subdomain auth
# Example: .myclinic.com (allows cookies to work across tenant1.myclinic.com, tenant2.myclinic.com, etc.)
# Leave empty for single-domain or local development
COOKIE_DOMAIN=
